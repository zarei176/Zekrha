# راهنمای مشارکت در پروژه ذکرهای من

از اینکه می‌خواهید در پروژه ذکرهای من مشارکت کنید، سپاسگزاریم! 🙏

## چگونه می‌توانم مشارکت کنم؟

### 🐛 گزارش باگ

اگر باگی پیدا کردید:

1. از [Issues](https://github.com/your-repo/issues) بازدید کنید
2. مطمئن شوید که باگ قبلاً گزارش نشده است
3. یک Issue جدید با برچسب `bug` ایجاد کنید
4. توضیحات کامل و مراحل بازتولید باگ را ارائه دهید

**قالب گزارش باگ:**
```markdown
**توضیحات باگ**
توضیحات واضح و مختصر از باگ

**مراحل بازتولید**
1. برو به '...'
2. کلیک کن روی '...'
3. پایین بیا به '...'
4. خطا را ببین

**رفتار انتظاری**
توضیح دهید که چه اتفاقی باید می‌افتاد

**اسکرین‌شات**
در صورت امکان، اسکرین‌شات اضافه کنید

**محیط:**
 - دستگاه: [مثلاً Samsung S21]
 - OS: [مثلاً Android 12]
 - نسخه اپ: [مثلاً 1.0.0]
```

### 💡 پیشنهاد ویژگی جدید

برای پیشنهاد ویژگی:

1. یک Issue با برچسب `enhancement` ایجاد کنید
2. توضیح دهید چرا این ویژگی مفید است
3. مثال‌های استفاده ارائه دهید

### 🔧 ارسال Pull Request

#### قبل از شروع

1. **Fork** کردن مخزن
2. **Clone** کردن fork خود
   ```bash
   git clone https://github.com/your-username/Zekrha.git
   ```
3. ایجاد **branch** جدید
   ```bash
   git checkout -b feature/amazing-feature
   ```

#### استانداردهای کد

- از **Dart/Flutter** style guide پیروی کنید
- کد باید **تمیز** و **خوانا** باشد
- همه متدها و کلاس‌ها باید **مستند** باشند
- از **نام‌گذاری فارسی** برای متغیرها استفاده کنید

مثال:
```dart
/// محاسبه تعداد کل اذکار انجام شده
/// 
/// این متد تمام جلسات را بررسی کرده و مجموع را برمی‌گرداند.
/// 
/// [dhikrId]: شناسه ذکر مورد نظر
/// Returns: تعداد کل اذکار انجام شده
int getTotalCountForDhikr(String dhikrId) {
  final sessions = _sessionBox.values.where((s) => s.dhikrId == dhikrId);
  return sessions.fold(0, (sum, s) => sum + s.currentCount);
}
```

#### تست

- برای تمام کدهای جدید **تست** بنویسید
- مطمئن شوید تمام تست‌ها **پاس** می‌شوند
  ```bash
  flutter test
  ```

#### Commit Message

پیام‌های commit باید واضح و توصیفی باشند:

```
[نوع]: عنوان کوتاه (حداکثر 50 کاراکتر)

توضیحات بیشتر در صورت نیاز.

- آیتم 1
- آیتم 2
```

**انواع:**
- `feat`: ویژگی جدید
- `fix`: رفع باگ
- `docs`: تغییرات مستندات
- `style`: تغییرات قالب‌بندی کد
- `refactor`: بازنویسی کد
- `test`: افزودن یا تغییر تست‌ها
- `chore`: سایر تغییرات

**مثال:**
```
feat: افزودن قابلیت ذکر سفارشی

- صفحه افزودن/ویرایش ذکر
- متدهای save و delete در Provider
- دکمه افزودن در کتابخانه اذکار
```

#### ارسال PR

1. **Push** کردن تغییرات
   ```bash
   git push origin feature/amazing-feature
   ```
2. به GitHub بروید و **Pull Request** ایجاد کنید
3. عنوان و توضیحات کامل ارائه دهید
4. منتظر بررسی بمانید

## 📋 چک‌لیست PR

قبل از ارسال PR، مطمئن شوید:

- [ ] کد از style guide پیروی می‌کند
- [ ] تست‌های مرتبط اضافه شده‌اند
- [ ] تمام تست‌ها پاس می‌شوند
- [ ] مستندات به‌روز شده‌اند
- [ ] CHANGELOG.md به‌روز شده است
- [ ] بدون conflict با branch اصلی

## 🎨 راهنمای طراحی UI

### رنگ‌ها

- **اصلی**: `#4CAF50` (سبز)
- **ثانویه**: `#009688` (آبی-سبز)
- **تاکید**: `#8BC34A` (سبز روشن)

### فونت‌ها

- **فارسی**: Vazirmatn
- **عربی**: Amiri

### فاصله‌ها

- **کوچک**: 8dp
- **متوسط**: 16dp
- **بزرگ**: 24dp

## 🔐 امنیت

اگر آسیب‌پذیری امنیتی پیدا کردید، لطفاً **به صورت خصوصی** گزارش دهید:
- ایمیل: security@zekrhaye-man.ir
- **از ایجاد Issue عمومی خودداری کنید**

## 📞 ارتباط

- **Issues**: برای باگ‌ها و پیشنهادات
- **Discussions**: برای سؤالات عمومی
- **Email**: support@zekrhaye-man.ir

## 📜 مجوز

با مشارکت در این پروژه، شما موافقت می‌کنید که مشارکت‌هایتان تحت [مجوز MIT](LICENSE) منتشر شود.

---

**از مشارکت شما متشکریم! 🌟**
